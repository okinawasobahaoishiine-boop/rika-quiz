# 理科一問一答の保守メモ

## ファイル構成

- `index.html`：HTMLの入口とライブラリの読み込み。公開URLは変更しない。
- `styles.css`：色・余白・文字サイズ・スマホ表示。
- `quiz-data.js`：通常851問と練習87問。各問題が専用の不正解候補3つを持つ固定データ。
- `app-config.js`：既存の保存キー、クラウド接続、問題選択・集計関数、バックアップ。
- `app.jsx`：Reactの画面と学習操作。Babel standaloneで読み込むためビルドは不要。
- `teacher.html`：既存の先生用ページ。今回変更なし。

## 生徒データを守るための条件

1. 既存と同じ公開URL・オリジンで更新する。別ドメインやローカルプレビューでは元のlocalStorageは表示されない。
2. `scienceQuizRecords`、名前、学年、プロフィール、連続学習、ベストタイム、端末ID、生徒コードのキーを変更しない。
3. 問題の `grade / field / level / no` の組を変更しない。過去の集計・復習と対応する。
4. Supabaseの接続先、認証セッション、`client_record_id` の生成方法、テーブル・列名を変更しない。
5. `index.html`と新しい4ファイルを同じ更新で公開する。HTMLだけを公開すると起動できない。
6. 開発・検証では架空のデータとクラウドのモックを使う。本番に試験記録を送信しない。

学習結果を追加するときは、端末の最新記録と画面内の記録をIDで併合する。不正なJSONや不正な履歴配列を検出した場合、既存値を上書きせず保存失敗を表示する。容量不足などで結果を保存できなかった場合は結果画面に残り、再保存できる。

これは同時書き込みの完全な排他制御ではない。複数タブが全く同時に保存する競合や、ブラウザのデータ消去・端末の故障までは防げない。バックアップと将来のアカウント復元機能で補う。

## バックアップ

生徒の「設定」から学習記録をJSONで保存できる。認証トークンは含めない。既存キーの生値を保存するため、手作業での復旧調査に利用できる。現時点で自動インポート・本人確認付き端末引き継ぎ機能は未実装。生徒コードだけで他の端末のデータを上書きする復元を追加しない。

## 2026-09-12の画面整理時の検証

旧HTMLと新しいデータ・設定ファイルを実行して、全問題データ、問題ID、保存キー、同じ入力に対するクラウド送信内容が一致することを確認した。

Edgeの独立したテスト用ブラウザで、架空の既存履歴を使って旧画面から更新した場合の保持、採点後の追記、再読み込み、バックアップ、通信失敗、容量不足、再保存、他タブで追加された履歴の保持、30問の満点テスト、記述・練習モードを確認した。PC・390pxスマホ画面を画像で確認した。

本番の生徒データの取得・削除・移行、GitHubへのpush、公開は行っていない。

## 2026-09-12の選択肢整理

全938問を見直し、774問の候補・問題文・解説を修正。うち9問は正解文言も修正した。画面整理時の「全問題データ一致」はこの内容改訂前の検証結果。

`smartWrongs` は各問題の `wrongs` を使い、別問題から候補を補充しない。正解1つと不正解3つが重複する不正な設定ではエラーとする。追加・編集時には同じ概念群の候補を用意し、問題文の条件で正解が1つに定まるようにする。問題IDを振り直さない。

最終確認を1回実施。全件の四択・ID・単元の整合性、保存処理の一致、架空データでの履歴保持と追記、再読み込み、記述・練習モード、スマホ表示を確認した。本番には未公開。
